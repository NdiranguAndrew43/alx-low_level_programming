#include "main.h"

/**
 * mul - code to multiply two random integers
 * @a: first random integer to be multiplied by second random integer
 * @b: second random integer to be multiplied by first random integer
 * Return: the result of multiplication between param a and param b
 */

int mul(int a, int b)

{
	return (a * b);
}

